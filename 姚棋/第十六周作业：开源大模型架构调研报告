
 第十六周作业：开源大模型架构调研报告

 一、Mistral Large 3（Mistral AI, 2025.12）

Mistral Large 3 是 Mistral AI 于 2025 年 12 月发布的开源旗舰模型，采用 Apache 2.0 协议。

核心架构：
 精细化混合专家（Granular MoE）：总参数量 6,750 亿，激活参数量 410 亿。无需为每个 token 启动所有神经元，仅激活最具影响力的部分。
 多模态原生支持：集成 25 亿参数的视觉编码器。
 上下文窗口：256K tokens。
 训练规模：在 3,000 台 NVIDIA H200 GPU 上从头训练。

关键技术特点：
 精细化 MoE 设计：相比传统 MoE，专家粒度更细，组合更灵活，在不牺牲准确性的前提下实现高效运作。
 全栈 NVIDIA 优化：利用 NVLink 内存一致性域和专家并行优化，配合 NVFP4 低精度推理及 Dynamo 分布式推理框架。在 GB200 NVL72 系统上相比 H200 性能提升 10 倍。
 多尺寸家族：同步发布 9 款小型密集模型（Ministral 3 系列），覆盖从云端到边缘的部署场景。
开源生态：已上线 Hugging Face、Amazon Bedrock、Microsoft Azure Foundry 等平台，NVIDIA TensorRTLLM、SGLang、vLLM 均已优化支持。
 二、LLaMA 3（Meta, 2024.04）

LLaMA 3 是 Meta 于 2024 年 4 月发布的新一代开源大模型系列，包含 8B 和 70B 两个主力版本。

核心架构：
 纯解码器 Transformer 架构：基于 Transformer 的自回归预测框架。
 分组查询注意力（GQA）：以 LLaMA 3 8B 为例，32 个 Q 头、8 个 KV 头（G=8），KV Cache 相比 MHA 缩减 4 倍。
 Tokenizer：词汇量 128K tokens。
 上下文长度：默认 8K，未来支持 128K。

关键技术特点：
 训练数据：总量达 15T tokens，是 LLaMA 2 的 7.5 倍，覆盖 30+ 种语言。
 训练基础设施：在定制 24K GPU 集群上运行，采用数据、模型、流水线三重并行，GPU 利用率超 400 TFLOPS。
 架构地位：LLaMA 3 的 Dense 架构已成为当前开源 Dense LLM 的标准结构。MoE 模型本质上是将 FFN 中的 SwiGLU 模块扩展为多个并联专家。

开源策略：全开源、商用免费，支持基础任务和对话任务（LLaMA 3Instruct）。

 三、Falcon 3（TII, 2025）

Falcon 3 是阿联酋技术创新研究所（TII）于 2025 年发布的开源模型家族，规模从 1B 到 10B 参数。

核心架构：
 纯解码器 Transformer 架构。
 分组查询注意力（GQA）：以 Falcon33B 为例，12 个 Q 头、4 个 KV 头。10B 版本含 40 个解码器块。
 上下文窗口：32K tokens（1B 版本为 8K）。
 许可证：FalconLLM 许可。

关键技术特点：
 定位清晰：专注于 1B10B 中小心模，适合个人电脑和边缘设备部署。
 极致压缩：提供 1.58bit 量化版本，实现极端压缩的同时保持较强性能。
 多框架支持：支持 MLX（Apple Silicon）、llama.cpp（CPU/GPU）、GGUF 格式。

 四、Gemma 2（Google DeepMind, 2024.06）
Gemma 2 是 Google DeepMind 基于 Gemini 同源技术构建的轻量级开源模型家族。
核心架构：
 Transformer 解码器框架，含多项增强。
 滑动窗口注意力 + 全局注意力交替：每层 50% 采用局部滑动窗口（4,096 token），50% 采用全局注意力。
 分组查询注意力（GQA）：2B 版本 8 头/4 组，9B 版本 16 头/8 组，27B 版本 32 头/16 组。
 序列长度：8,192 tokens。

关键技术特点：
 Logit Softcapping：注意力 logits 限制在 50.0，最终输出 logits 限制在 30.0，防止极端值。
 GeGLU 激活：门控线性单元配合 GELU 激活。
 RMSNorm + RoPE。
 知识蒸馏：较小版本（2B/9B）从 27B 模型蒸馏。
 效率优势：27B 版本可在单块 H100 GPU 全精度推理，计算资源消耗降低 50%。

可解释性工具：发布 Gemma Scope 工具套件，提供数百个可免费获取的稀疏自编码器。

 五、Phi4（Microsoft, 2025.01）

Phi4 是微软 Phi 家族的最新成员，14B 参数，MIT 开源协议。

核心架构：
 稠密解码器only Transformer（基础版）。
 混合专家架构（MoE）：MoE 层含 8 个专家模块，通过门控网络动态路由。
 上下文长度：16K tokens。
 训练数据：9.8T tokens，包含合成数据、过滤后的公共领域网站、学术书籍和问答数据集。

关键技术特点：
 混合专家优化：动态路由机制，复杂逻辑推理自动激活数学专家，文本生成调用语言理解专家。
 数据工程：多阶段数据筛选——基础数据层（代码/数学/科学文献）、强化数据层（10 万条高难度推理题）、对抗数据层（噪声与歧义）。
 分阶段学习率：前 30% 高学习率快速收敛，中间 50% 逐步降低精细调优，最后 20% 微调巩固。
 原生量化支持：4 位量化体积压缩至 1/8（约 7GB），推理速度提升 2.3 倍。

多模态版本：Phi4multimodal 包含六个组件：Phi4mini 基座、视觉编码器、语音编码器及对应投影器，以及两个 LoRA 适配器。

混合架构变体：Phi4miniflashreasoning 采用 DecoderHybridDecoder 架构（SambaY），核心创新是门控记忆单元（GMU），结合 Mamba（状态空间模型）和滑动窗口注意力（SWA），再加单层全注意力。

 六、DeepSeekV3（DeepSeek, 2025.03 更新）

DeepSeekV3 是深度求索的开源 MoE 旗舰模型。

核心架构：
 混合专家（MoE）：总参数 671B/685B，每个 token 激活 37B 参数。
 256 个专家：根据问题选择 8 位专家协作。
 多头潜在注意力（MLA）：降低推理时 KV 缓存，提升推理效率。
 MTP（MultiToken Prediction）：增加训练信号密度。
 上下文窗口：128K。
 开源许可：MIT。

关键技术特点：
 无辅助损失负载平衡：动态调整专家负载。
 开源权重包含 MTP 模块。
 分布式推理：将 embedding、MLA、MoE 模块中的线性层平分到所有卡。

 七、Qwen2.5（Alibaba, 2025.01）

Qwen2.5 是阿里云通义千问团队发布的全面开源模型系列。

核心架构：
 纯解码器 Transformer：与 Qwen2 架构一致。
 分组查询注意力（GQA）。
 SwiGLU 激活。
 RoPE 旋转位置编码。
 RMSNorm 预归一化。
 QKV Bias：注意力中保留 QKV 偏置。

模型规模：开源密集模型覆盖 0.5B、1.5B、3B、7B、14B、32B、72B 七个尺寸。72B 版本层数 L=80，隐层维度 H=8192。

训练数据：预训练数据从上一代的 7T tokens 扩展至 18T tokens。后训练包含超 100 万样本的 SFT，以及多阶段强化学习（离线 DPO + 在线 GRPO）。

多模态与 MoE：
 Qwen2.5Omni：支持多模态。
 MoE 版本：Qwen2.5Turbo 和 Qwen2.5Plus 通过阿里云 API 提供。
 开源版本均为密集模型。

 八、横向对比总结
模型	总参数	激活参数	架构类型	注意力机制	上下文	特色
Mistral Large 3	675B	41B	Granular MoE	精细化 MoE	256K	多模态原生 + NVFP4
LLaMA 3-70B	70B	70B	Dense	GQA	8K→128K	15T tokens 训练
Falcon 3-10B	10B	10B	Dense	GQA	32K	1.58bit 极致压缩
Gemma 2-27B	27B	27B	Dense	GQA + 滑动窗口	8K	Logit Softcapping
Phi-4	14B	14B	Dense/MoE	混合注意力	16K	8 专家 MoE + 4bit 量化
DeepSeek-V3	671B	37B	MoE	MLA	128K	256 专家 + 无辅助损失
Qwen2.5-72B	72B	72B	Dense	GQA	128K	18T tokens 训练

 演进趋势观察

1. MoE 稀疏化成为旗舰标配：Mistral Large 3、DeepSeekV3 等超大模型均采用 MoE，总参数向 T 级迈进，激活参数控制在 40B 以内。
2. GQA 成为 Dense 模型标配：LLaMA 3、Falcon 3、Gemma 2、Qwen2.5 均采用 GQA 降低 KV Cache。
3. 轻量级模型百花齐放：Falcon 3（110B）、Gemma 2（227B）、Phi4（14B）在保持高性能的同时追求极致效率。
4. 多模态原生集成：Mistral Large 3 内置视觉编码器，Phi4 提供多模态版本。
5. 量化与压缩成为核心设计：FP4/INT4 量化从“事后优化”变为“原生支持”。
